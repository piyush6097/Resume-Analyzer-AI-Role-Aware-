# Resume Analyzer AI — Role Aware

## Run locally
```bash
python -m venv .venv && . .venv/Scripts/activate  # on Windows
pip install -r requirements.txt
cp .env.example .env  # fill values
python app.py
